import React from "react";
import { Formik, Field, Form } from "formik";
import styles from "./letter.module.scss";
import { NavLink } from "react-router-dom";
import Button from "../../../../common/button";

const Letter = () => {
  const handleSubmit = (values, actions) => {
    console.log(values); // Access form values here
    // Add your submission logic here
    actions.setSubmitting(false); // Set form as submitted
  };

  return (
    <Formik
      initialValues={{
        fromCity: "",
        fromProvince: "",
        fromCountry: "",
        packageWeight: "",
        packageUnit: "kg",
        toCity: "",
        toProvince: "",
        toCountry: "",
        insuranceAmount: "",
        currency: "CAD",
        agreeTerms: false
      }}
      onSubmit={handleSubmit}
    >
      {({ isSubmitting }) => (
        <Form className={`${styles.form} form `}>
          {/* Your form JSX with Formik components */}
          {/* Use Field components to connect inputs to form state */}
          {/* Example: */}
          <div className={`${styles.container} d-flex-r`}>
            <div className={`${styles.innerContainer} d-flex-col w-50 `}>
              <div className={`${styles.fromDiv} `}>
                <h6>Shipping From</h6>
                <Field type="text" name="fromCity" placeholder="Search City/ Postal Code" />
                <div className={`${styles.portalContent}  d-flex-r`}>
                  <Field type="text" name="fromCity" placeholder="city" />
                  <Field type="text" name="fromProvince" placeholder="Province" />
                  <Field type="text" name="fromCountry" placeholder="Country" />
                </div>
              </div>
              <div>
                <h6>Package Details</h6>
                <div className="d-flex-r">
                  <div className="d-flex-col">
                    <label htmlFor="packageWeight">Weight</label>
                    <Field type="number" name="packageWeight" />
                  </div>
                  <div className="d-flex-col">
                    <label htmlFor="packageUnit">Unit</label>
                    <Field as="select" name="packageUnit">
                      <option value="kg">kg</option>
                      <option value="LBS">LBS</option>
                    </Field>
                  </div>
                </div>
              </div>
            </div>
            {/* ... */}
          </div>
          {/* ... */}
          <Button className={`${styles.submitBtn}`} type="submit" value="Get Quote" disabled={isSubmitting} />
        </Form>
      )}
    </Formik>
  );
};

export default Letter;
